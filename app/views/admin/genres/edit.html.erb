<h3>ジャンル編集</h3>

<%= form_with model: [:admin,@genre], url: admin_update_genre_path(@genre.id), method: :patch  do |f| %>
  <h6>ジャンル名</h6>
  <%= f.text_field :name %>
  <%= f.submit "変更を保存" %>
<% end %>